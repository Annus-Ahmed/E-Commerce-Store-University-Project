{
  "name": "pre-owned-marketplace",
  "version": "1.0.0",
  "description": "A marketplace application for buying and selling pre-owned items",
  "main": "backend/index.js",
  "scripts": {
    "setup": "node setup.js",

    "dev": "concurrently \"cd backend && npm start\" \"cd frontend && npm start\"",

    "start": "cd backend && npm start",

    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "marketplace",
    "ecommerce",
    "mern"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "bcryptjs": "^3.0.2",
    "mongoose": "^8.14.1"
  },
  "devDependencies": {
    "concurrently": "^7.6.0"
  }
}
